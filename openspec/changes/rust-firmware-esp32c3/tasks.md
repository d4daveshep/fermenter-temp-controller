# Tasks: rust-firmware-esp32c3

## 1. Workspace scaffold

- [x] 1.1 Create `firmware/` directory with a Cargo workspace `Cargo.toml` declaring members `["logic", "esp32c3"]`
- [x] 1.2 Create `firmware/logic/Cargo.toml` as a `no_std`-compatible library crate; add `rstest` as a dev-dependency
- [x] 1.3 Create `firmware/esp32c3/Cargo.toml` targeting `riscv32imc-unknown-none-elf`; declare `logic` as a path dependency; add `esp-hal`, `esp-rtos` (with the `embassy` feature enabled), `embassy-executor`, `embassy-time`, `embedded-hal`, `embedded-io-async`, `onecable` (OneWire/DS18B20 — swapped in for the originally proposed `one-wire-bus`/`ds18b20`, which need `embedded-hal 0.2` with no `esp-hal 1.1` compatibility path; see design.md Decision 12), `serde-json-core`, `heapless`, `esp-backtrace` (panic handler), `esp-bootloader-esp-idf` (app descriptor). `esp-println` is a plain (non-optional) dependency with `default-features = false, features = ["jtag-serial"]`: it turns out `esp-backtrace`'s `println` feature already pulls `esp-println` in unconditionally for panic output and needs a transport feature selected regardless of our own gate, so making our own reference to it `optional` bought nothing — the `debug-log` feature (off by default — see design.md Decision 7) instead just gates our own `esp_println::println!` call sites behind `#[cfg(feature = "debug-log")]`. Verified by actually compiling against real `esp-hal 1.1.1`/`esp-rtos 0.3.0` (`rustup target add riscv32imc-unknown-none-elf` — no `espup` needed for this RISC-V target).
- [x] 1.4 Create `firmware/esp32c3/.cargo/config.toml` with target triple, `espflash flash --monitor` as the runner, and `rustflags` containing both `-C link-arg=-Tlinkall.x` (pulls in esp-hal's generated linker script — required, or linking fails with undefined-symbol errors for peripheral/interrupt-vector symbols like `GPIO`, `WIFI_MAC`, `__EXTERNAL_INTERRUPTS`) and `-C force-frame-pointers` (needed for usable `esp-backtrace` panic output on RISC-V). Confirmed by an actual `cargo build --release` + link + flash + echo round trip in task 10.2.
- [x] 1.5 Create `firmware/README.md` documenting toolchain setup (`espup`, `espflash`), how to run `cargo test` for the logic crate, the sensor ROM address discovery procedure, and the `debug-log` feature (what it does, and that it must stay off when connected to a live `fermenter/` host — see design.md Decision 7)

## 2. `firmware-logic`: Action and Decision types (TDD)

- [x] 2.1 Write failing tests for `Action` enum variants and `Decision` struct: `stores_action_and_reason`, `action_text_for_each_variant`, `is_made_returns_false_before_set`, `is_made_returns_true_after_set`, `clear_resets_to_unmade`
- [x] 2.2 Implement `Action` enum (`Rest`, `Heat`, `Cool`, `Error`, sentinel) and `Decision` struct (`action`, `reason_code`, `action_text()`, `is_made()`, `clear()`) in `firmware/logic/src/decision.rs` to make the tests pass

## 3. `firmware-logic`: Natural drift classification (TDD)

- [x] 3.1 Write failing tests for `NaturalDrift` classification: ambient equals fermenter → `NaturalHeating`, ambient above fermenter → `NaturalHeating`, ambient below fermenter → `NaturalCooling`
- [x] 3.2 Implement `NaturalDrift` enum and `get_natural_drift(ambient: f64, actual: f64) -> NaturalDrift` in `firmware/logic/src/controller_action_rules.rs` to make the tests pass

## 4. `firmware-logic`: Failsafe boundary enforcement (TDD)

- [x] 4.1 Write failing tests for failsafe: all 6 combinations of `(REST|HEAT|COOL) × (ambientLow|ambientHigh)` when temp is below failsafe → `Heat`/`"RC1"`; same matrix when temp is above failsafe → `Cool`/`"RC5"` (porting the assertions from `Test_ControllerActionRules.cpp` tests 1, 5, 6, 10)
- [x] 4.2 Implement `check_failsafe_min` and `check_failsafe_max` in `ControllerActionRules` to make the tests pass, using `TARGET_RANGE = 0.3` (failsafe is `target ± 2 × TARGET_RANGE`, i.e. `± 0.6`) per design.md Decision 9

## 5. `firmware-logic`: Cooling overrun adjustment (TDD)

- [x] 5.1 Write failing tests for cooling overrun: `COOL + temp < stop_cooling_temp + NaturalHeating → Rest/"RC2.2"` and `COOL + temp < stop_cooling_temp + NaturalCooling → Heat/"RC7.2"` (porting `AdjustmentForCoolingOverrun` and the RC2.2/RC7.2 cases in `WhatToDoNext`)
- [x] 5.2 Implement `check_cooling_overrun` and `get_stop_cooling_temp` in `ControllerActionRules` to make the tests pass

## 6. `firmware-logic`: Temperature range decision rules (TDD)

- [x] 6.1 Write failing tests for below-range decisions: all four cases RC2.1, RC2.3, RC7.1, RC7.3 (porting the RC2 and RC7 assertions from `WhatToDoNext`)
- [x] 6.2 Implement `decide_when_below_range` to make the tests pass
- [x] 6.3 Write failing tests for in-range decisions: all six cases RC3.1, RC3.2, RC3.3, RC8.1, RC8.2, RC8.3
- [x] 6.4 Implement `decide_when_in_range` to make the tests pass
- [x] 6.5 Write failing tests for above-range decisions: all six cases RC4.1, RC4.2, RC4.3, RC9.1, RC9.2, RC9.3
- [x] 6.6 Implement `decide_when_above_range` to make the tests pass

## 7. `firmware-logic`: Target temperature mutability (TDD)

- [x] 7.1 Write failing test: `set_target_temp` persists and is returned by `get_target_temp`; subsequent `make_action_decision` uses the new value (porting `UpdatedTargetTempIsSaved`)
- [x] 7.2 Implement `set_target_temp` / `get_target_temp` and wire the top-level `make_action_decision` method that orchestrates all the sub-checks, to make the tests pass; construct `ControllerActionRules` with `DEFAULT_TARGET_TEMP = 20.0` and `TARGET_RANGE = 0.3` per design.md Decision 9 / `HARDWARE.md`

## 8. `firmware-logic`: Exponential moving average (TDD)

- [x] 8.1 Write failing tests for `TemperatureReadings`: `seed_sets_initial_average`, `seed_ignored_after_first_reading`, `ema_matches_expected_value_over_known_sequence`, `min_and_max_tracked_correctly`, `count_increments_with_each_update` (porting the AUnit `TemperatureReadingsTest` suite with the same known random seed and expected EMA value `12.14163`)
- [x] 8.2 Implement `TemperatureReadings` struct with `set_initial_average`, `update`, `average`, `minimum`, `maximum`, `count` in `firmware/logic/src/temperature_readings.rs` to make the tests pass

## 9. `firmware-logic`: Verify complete logic test coverage

- [x] 9.1 Run `cargo test` in `firmware/logic/` — all tests green, no warnings
- [x] 9.2 Run `cargo clippy -- -D warnings` in `firmware/logic/` — clean
- [x] 9.3 Run `cargo fmt --check` in `firmware/logic/` — clean

## 10. `firmware-device`: Hardware bring-up — serial echo

- [x] 10.1 Create `firmware/esp32c3/src/main.rs` with a `#[esp_rtos::main]` async entry point; call `esp_rtos::start(timg0.timer0, sw_int.software_interrupt0)` before spawning any task (see design.md Decision 2); add a USB-Serial-JTAG echo task using `esp-hal`'s `UsbSerialJtag` peripheral
- [x] 10.2 Flash to the test board and verify the echo works over `/dev/ttyACM0` — confirms toolchain, flash, and USB-Serial-JTAG are all functional. Done via `cargo build --release` + `espflash flash --non-interactive` (not `cargo run`'s configured runner directly — that runner appends `--monitor`, which blocks forever and isn't scriptable; use `cargo run --release` interactively instead when at a real terminal). Verified with a small pyserial script sending three round-trip messages (plain text, an RC-code-shaped string, and a `<19.5>`-framed value) — all three echoed back byte-for-byte. `firmware/esp32c3/.cargo/config.toml` was missing the linker-script rustflag (`-C link-arg=-Tlinkall.x`) needed to resolve esp-hal's peripheral/interrupt-vector symbols — added, confirmed by an actual successful link + flash + echo.

## 11. `firmware-device`: Sensor ROM address discovery

- [x] 11.1 Create `firmware/esp32c3/examples/discover_sensors.rs` that scans the OneWire bus and prints the ROM address of every DS18B20 found, then halts. Uses `onecable::OneWire::search_rom_iter` (see design.md Decision 12) against an `esp_hal::gpio::Flex` pin configured as true open-drain input+output (`DriveMode::OpenDrain`, `Pull::None` — matches the external 4.7kΩ pull-up). Verified compiling and linking for real.
- [x] 11.2 Wire the DS18B20 sensor(s) to the chosen OneWire GPIO pin (GPIO4 — see HARDWARE.md) with a 4.7kΩ pull-up; flash the discovery example and record ROM addresses. Both sensors are now wired to the production board (shared bus, single pull-up). Initial discovery hung indefinitely after "Scanning..." — root-caused to `onecable::OneWire::initialize_bus`'s unbounded `while pin.is_low()` wait with no timeout, triggered by a wiring issue; resolved after rechecking the physical wiring. Wrote `examples/identify_sensors.rs` (discovery + continuous per-ROM live temperature loop, since two sensors on one bus need a way to tell which physical sensor is which) and used it to discover both: `rom=820000073792F228` and `rom=52000009B5F2F728` (both valid CRC, family 0x28). Physically identified by warming the sensor wired near the fermenter vessel and watching which ROM's reading rose.
- [x] 11.3 Add `FERMENTER_SENSOR_ADDR: [u8; 8]` and `AMBIENT_SENSOR_ADDR: [u8; 8]` compile-time constants in `firmware/esp32c3/src/sensors.rs` with the discovered values; document the discovery step in `firmware/README.md`. Both constants now hold real, physically-confirmed ROM addresses: `FERMENTER_SENSOR_ADDR = 820000073792F228`, `AMBIENT_SENSOR_ADDR = 52000009B5F2F728`. `firmware/README.md`'s status note updated accordingly, plus a callout about the `initialize_bus` no-timeout hang risk for future bring-up.

## 12. `firmware-device`: GPIO pin assignment and relay control

- [x] 12.1 Confirm free GPIO pins on the board silkscreen/schematic; record `HEAT_RELAY_PIN`, `COOL_RELAY_PIN`, `ONE_WIRE_PIN` as named constants in `firmware/esp32c3/src/relays.rs` and `sensors.rs`, verifying none are GPIO2, GPIO8, or GPIO9. `HEAT_RELAY_PIN`/`COOL_RELAY_PIN` = GPIO0/GPIO1 (proposed, per HARDWARE.md); these are documentation/cross-reference constants — the actual peripheral selection happens via `peripherals.GPIO0`/`GPIO1`'s distinct esp-hal types, not a runtime pin-number lookup. Added a `src/lib.rs` (auto-detected by Cargo alongside `main.rs`'s `[[bin]]`) so `relays`/`sensors` modules are shared between `main.rs` and `examples/*.rs` without duplication.
- [x] 12.2 Implement `RelayController` in `firmware/esp32c3/src/relays.rs`: owns the two `Output` GPIO handles; `set(action: Action)` drives them per the spec (Rest → both LOW, Heat → heat HIGH cool LOW, Cool → heat LOW cool HIGH, Error → both LOW; `NoAction` sentinel also treated as fail-safe both-LOW, defensively). Verified compiling and linking for real.
- [x] 12.3 Flash a relay test to the board (cycle through Rest → Heat → Cool → Rest once per second); verify with a multimeter or LED that pin logic matches spec. Flashed via `cargo build --release --example relay_test` + `espflash flash --non-interactive <path>` (the `cargo run` runner's `--monitor` blocks forever, same issue as 10.2); no relays/LEDs needed after all — probed GPIO0/GPIO1 directly against GND with a multimeter. Confirmed on real hardware (2026-08-01): both pins go HIGH for ~1s in the expected Rest/Heat/Cool/Rest cycle. Re-confirmed with the actual relays wired up (2026-08-01): both clicked in the correct Rest/Heat/Cool/Rest order.

## 13. `firmware-device`: DS18B20 temperature sensor reads

- [x] 13.1 Implement `SensorReader` in `firmware/esp32c3/src/sensors.rs`: initialises the OneWire bus, locates each sensor by its ROM address, triggers conversion, and reads back both temperatures as `Result<(f64, f64), SensorError>` (fermenter, ambient) — do not silently coerce a failed read into a bogus float; see design.md Decision 10. `SensorReader::new` owns the `Flex` OneWire pin and applies the same open-drain config as `examples/discover_sensors.rs`; `read()` addresses each sensor directly via `onecable`'s `DS18B20::try_from(RomCode)` + `read_temperature` (match-rom, no bus scan needed since the ROM is already known) and converts the returned `FixedI16<U4>` to `f64` via the `fixed` crate's `to_num` (added as a new dependency, version-matched to `onecable`'s own). `SensorError::{Fermenter,Ambient}` wraps a `SensorFault` (`NotADs18b20` for a family-code mismatch — e.g. today's placeholder all-zero ROM constants — or `Bus` for a CRC/presence-pulse failure), so with the current placeholder addresses a real read attempt fails safe rather than returning bogus data. Verified compiling and linking for real (`cargo build --release`), plus clippy/fmt clean; confirmed against real hardware in 13.2.
- [x] 13.2 Flash a sensor-read loop to the board; verify both temperatures are printed over USB-Serial-JTAG — confirms OneWire timing, pull-up resistor, and ROM address constants are correct. Wrote `examples/sensor_read_test.rs`, calling `SensorReader::read()` directly (the same code path task 16's main loop will use) once per second. Flashed to the production board with the real `FERMENTER_SENSOR_ADDR`/`AMBIENT_SENSOR_ADDR` constants from 11.3: printed `fermenter=32.56C ambient=16.19C` and climbing/settling over subsequent reads (fermenter still cooling from the fingertip warming used to identify it in 11.2), no `SensorError`s. Confirms `SensorReader` — not just the raw `onecable` calls `identify_sensors.rs` exercises — works end-to-end on real hardware.

## 14. `firmware-device`: Target frame parser (TDD in `logic`, integration test on device)

- [x] 14.1 Write failing tests in `firmware/logic/src/protocol.rs` (or a host-side test module): `valid_frame_returns_some_value`, `zero_frame_returns_some_zero` (not None), `no_frame_returns_none`, `partial_frame_returns_none`, `malformed_frame_returns_none`. Confirmed failing to compile (`parse_target_frame` not yet defined) before implementing.
- [x] 14.2 Implement `parse_target_frame(buf: &[u8]) -> Option<f64>` in `firmware/logic/src/protocol.rs` to make the tests pass. Finds the first `<`...`>` pair via `position`, parses the inner UTF-8 slice with `core`'s built-in `str::parse::<f64>()` (no dependency needed, works in `no_std`) — `0.0` parses to `Some(0.0)` same as any other value, fixing the Arduino's zero-target bug simply by not special-casing it. All 5 tests pass; full `logic` suite (55 tests), clippy, and fmt all clean. Exported from `lib.rs`; `esp32c3` device crate still builds against the new `logic` API.
- [x] 14.3 Wire `parse_target_frame` into the USB-Serial-JTAG RX path in `esp32c3/src/main.rs`, wrapped in a persistent `heapless::Vec<u8, 16>` accumulator that carries unconsumed bytes across ticks so a frame split across a tick boundary is not lost (see design.md Decision 11 — mirrors the Arduino's stateful `recvInProgress`/`ndx`); flash to the test board and send `<19.5>` from a terminal — verify the target updates in the printed log. `frame_buf`/`frame_in_progress` are plain locals in `echo_task` (persist for the task's lifetime, same as the Arduino statics); on `<` the accumulator resets and starts capturing, on `>` it calls `parse_target_frame` and resets again regardless of parse success, and a push past the 16-byte capacity abandons the in-progress frame rather than growing unbounded. The `target updated: {value}` print stays behind `#[cfg(feature = "debug-log")]` (Decision 7 — this file runs concurrently with a real `fermenter/` host, unlike the standalone examples), with the non-debug-log build explicitly discarding the parse result (`let _ = parsed;`) to stay clippy-clean in both configs. Verified on real hardware, flashed with `--features debug-log`: sending `<19.5>` in one write printed `target updated: 19.5`; sending `<12.` and `25>` as two separate writes (forcing two separate `rx.read` calls) printed `target updated: 12.25`, confirming the accumulator survives a split frame; sending `<0.0>` printed `target updated: 0` — the zero-target fix, not silently dropped.

## 15. `firmware-device`: JSON telemetry output (TDD in `logic`, integration test on device)

- [x] 15.1 Write failing tests in `firmware/logic/src/protocol.rs` for `format_telemetry`: emitted JSON contains all required keys (`target`, `average`, `min`, `max`, `ambient`, `action`, `reason-code`); `action` strings match host-expected values; `reason-code` is byte-identical to the RC code; output is newline-terminated. Added a `TelemetryData` struct (`#[derive(Serialize)]`, `action`/`reason_code` as `&'static str` matching `Decision::action_text()`/`reason_code()`) alongside the tests, since the tests need something to construct — confirmed failing to compile (`format_telemetry` not yet defined) before implementing.
- [x] 15.2 Implement `format_telemetry(readings: &TelemetryData) -> heapless::String<256>` using `serde-json-core` to make the tests pass. Added `serde`, `serde-json-core`, and `heapless` to `firmware/logic/Cargo.toml` — `heapless` pinned to `"0.8"` (not `"0.9"` like `esp32c3`'s own) specifically to match `serde-json-core` 0.6's internal `heapless = "0.8"` dependency, so the `heapless::String<256>` this function returns is Cargo's single unified 0.8 instance, not a second incompatible type; `esp32c3`'s separate heapless 0.9 usage is unaffected since Cargo builds distinct instances per major version (same pattern `onecable`'s internal heapless 0.8 already established). All 4 new tests pass; full `logic` suite (59 tests), clippy, fmt all clean; `esp32c3` still builds against the updated `logic` API.
- [x] 15.3 Wire `format_telemetry` into the Embassy task's 10-second emission path; flash to the test board and verify the JSON output is parseable by the `fermenter/` host's `Reading` deserialiser (use `serde_json::from_str::<Reading>` in a small CLI or REPL). `echo_task` now `select`s between `rx.read` and an `embassy_time::Ticker::every(10s)` (added `embassy-futures` as a direct dependency) rather than a sequential `Timer::after` re-armed each loop pass, which would've reset on every incoming byte instead of firing on a true fixed interval — still one task per design.md Decision 2. Emitted values are fixed placeholders (`target:19.5, average:18.2, min:18.0, max:18.4, ambient:20.1, action:"Rest", reason-code:"RC3.1"`, matching design.md Decision 5's sample exactly) — real sensor reads and a real decision are task 16's job; this step only proves the periodic-emission wiring and wire-format correctness. Verified on real hardware: captured a clean run of newline-delimited JSON lines at the expected cadence, then fed one through a throwaway scratch Cargo project depending on `fermenter` as a path dependency (no changes to `fermenter/` itself, matching design.md's Impact section) that called `serde_json::from_str::<fermenter::model::Reading>` on it directly — parsed successfully with all fields mapped correctly (`json_size: None` as expected, since the firmware doesn't emit that legacy field).

## 16. `firmware-device`: Complete main control loop

- [x] 16.1 Assemble the full Embassy task loop in `firmware/esp32c3/src/main.rs`: 1s tick → poll serial RX for `<target>` frame → read both sensors → update EMAs → `make_action_decision` → `relay_controller.set(action)` → emit JSON every 10 ticks; seed both EMAs on startup from first sensor read; construct the fermenter `TemperatureReadings` with window `60` and the ambient one with window `10` (design.md Decision 9); if `SensorReader` returns `Err`, skip `make_action_decision` for that tick and call `relay_controller.set(Action::Error)` directly (design.md Decision 10). `main.rs`'s bring-up `echo_task` (tasks 10/14/15) is fully replaced by `control_task`, which drops the echo-back behaviour entirely (never part of the real protocol) and `select`s each iteration between `rx.read` and a 1-second `Ticker`, same pattern as task 15.3's periodic emission. `current_action` is a plain local (persists for the task's lifetime), initialised to `Action::Rest` matching the Arduino's `Action currentAction = REST` startup default. A failed initial seed read (device just booted, sensors not yet responsive) is handled by simply leaving both `TemperatureReadings` at their zeroed default rather than propagating an error — not explicitly specified by design.md, but a reasonable extension of Decision 10's spirit for a case Decision 10 didn't cover (a startup-only failure, not a steady-state tick failure). Builds, clippy, and fmt all clean in both the default and `debug-log` feature configurations.
- [x] 16.2 Flash to the test board; connect to `fermenter/` host with `MOCK_SERIAL=false`; confirm readings are ingested, dashboard updates, and `POST /target` causes the board to apply the new target within one loop cycle. Verified on real hardware end-to-end: flashed the production board, ran `docker compose up --build` (a stale `fermenter:arm64` image left over from a prior cross-build had to be rebuilt for the dev host's actual platform first — `exec format error` until then) with the existing `fermenter/.env` (`MOCK_SERIAL=false`, `SERIAL_PORT=/dev/ttyACM0`). Host correctly skipped the ESP32-C3's USB-reset boot-log noise as malformed lines, then ingested real readings (`reading ingested target=20.0 average=15.13 action=Heat`), auto-reconciled its persisted target against the board's firmware-default startup value (`desired=19.5 reported=20.0` → next reading showed `target=19.5`), rendered the live dashboard (`curl localhost:8080/` showed matching action/reason/target/average/min/max/ambient), and a manual `POST /target` (19.5 → 21.0) was accepted, written back, and confirmed applied on the very next ingested reading (~16s later, well within expectations for a 1s tick + up to 10s telemetry cadence).

## 17. End-to-end hardware validation

- [x] 17.1 Run `fermenter/` against the test board for at least 10 minutes; confirm readings, relay switching, and target reconcile all behave as expected; verify `reason-code` values appear correctly in the dashboard. Run against the production board directly (sensors/relays already the production set since the task 11-13 hardware sessions) with `MOCK_SERIAL=false`: 604s continuous run, 218 readings ingested, zero errors/warnings beyond the expected one-time boot-reset noise (opening the serial port resets the ESP32-C3, producing ~30 malformed-line skips at startup only), no crashes or reconnects. Dashboard correctly rendered the live reason-code description (`RC1 — HEAT because the temperature is below the failsafe minimum...`). Target reconcile (confirmed already in task 16.2) held steady throughout. Observation (not a defect, not fixed): telemetry lands every ~15-16s rather than the nominal 10s, because `SensorReader::read()` blocks for ~1.5s+ per tick (two sequential DS18B20 conversions at 750ms each per design.md's `TempPrecision::Bits12`), stretching each nominal "1-second" tick to ~1.5-2s wall-clock — correspondingly, the EMA windows (Decision 9's 60/10 sample counts) span more real time than their names suggest (~90-120s and ~15-20s respectively). Harmless for this application; worth knowing if tighter timing ever matters.
- [ ] 17.2 Flash the production board with the finalised binary; swap for the Arduino Uno; repeat the 10-minute validation
- [x] 17.3 Add a `#[test] #[ignore]` hardware integration test in `firmware/esp32c3/tests/hardware.rs` (or `firmware/tests/`) that opens the serial port, reads one JSON line, and verifies it deserialises as a valid `Reading` — to be run manually with `cargo test -- --ignored` when the board is attached. Placed in `firmware/logic/tests/hardware.rs` instead, not `esp32c3/` — `esp32c3`'s `.cargo/config.toml` pins every cargo invocation to the `riscv32imc-unknown-none-elf` embedded target and its lib links `esp-hal`/`esp-rtos`/`onecable`, none of which build for a host target, so it can't host a test that needs a real host-side serial port; `logic` already runs its tests on the host with no embedded toolchain (`cfg_attr(not(test), no_std)`), so no target override is needed there. Added `serialport`/`serde_json` as dev-dependencies (test-only — doesn't affect `logic`'s no_std production build) and a local `Reading`-mirroring struct rather than a dependency on the `fermenter` crate itself, keeping `firmware/` independent per repo root CLAUDE.md; mirrors the retry-until-timeout structure already established in `fermenter/tests/serial_hardware.rs`'s `reads_a_real_line` test. Verified: compiles clean, `cargo test` shows it correctly skipped (`1 ignored`), clippy/fmt clean, and `cargo test -- --ignored` passes against the real attached board.

## 18. CI and tooling

- [x] 18.1 Add a GitHub Actions job to `.github/workflows/rust.yml` that runs `cargo test` and `cargo clippy -- -D warnings` in `firmware/logic/` (host target, no embedded toolchain required). Added `firmware-logic` job (`working-directory: firmware/logic`, `Swatinem/rust-cache` pointed at the `firmware` workspace root) running `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test`, mirroring the three checks `firmware/README.md` already documents for local use; `tests/hardware.rs`'s one test stays `#[ignore]`'d and excluded, same as `fermenter/`'s hardware tests. Verified all three commands pass locally and the YAML parses.
- [x] 18.2 Add a separate CI job (or step) that installs the RISC-V target and attempts `cargo build --release` in `firmware/esp32c3/` (compile-only, no flash) to catch regressions in the device crate. Added `firmware-esp32c3` job, `targets: riscv32imc-unknown-none-elf` on the `dtolnay/rust-toolchain` action (plain `rustup target add`, no `espup` needed for RISC-V per design.md), compile-only `cargo build --release` (`.cargo/config.toml` already pins the target so no `--target` flag needed). Verified locally.
- [x] 18.3 Confirm `arduino/TempController/compile_arduino.sh` and `upload_arduino.sh` still work (no changes made to the Arduino tree) — leave them in place as documented historical reference. `git diff`/`git log` confirm zero commits on this branch touched `arduino/`. Actually ran `compile_arduino.sh`: failed, but with `/opt/arduino-1.8.19/java/bin/java: cannot execute binary file: Exec format error` — a pre-existing local-environment issue (the bundled Arduino IDE's `java` binary doesn't match this host's architecture), unrelated to and predating this change; not a regression from the firmware work.
